// TODO: remove this example test
import {expect} from 'chai';
import {hello} from '../src/lib/index';

describe('example test', () => {
	it('shows how to make a test', () => {
		expect(hello()).to.eq('world!');
	});
});
