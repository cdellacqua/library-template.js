// TODO: use your library in this file, this is the script that runs in the demo page (npm run dev:browser)
import {hello} from './lib';

document.body.appendChild(document.createTextNode('hello, ' + hello()));
